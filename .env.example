# Local-only environment variables (copy to .env; never commit .env)
# Optional data root for generation / path remapping helpers
LITE_AIGC_DATA_ROOT=
LITE_AIGC_ROOT=
# Optional Hugging Face token for downloading public model cards (local use only)
HF_TOKEN=
# Do NOT put cloud SSH hosts/passwords in the public tree.
